# ADR-001：采用领域导向模块化单体

- 状态：Accepted
- 日期：2026-07-13

## 背景

项目需要展示企业级边界、事务、事件和可运行性，但由单人主要维护，无独立团队/发布/伸缩证据。

## 决策

采用一个 Spring Boot 部署单元和多个 Spring Modulith 业务模块。模块代码、数据、事件和公开 API 严格隔离。

## 后果

优点：本地启动简单、本地事务清晰、审阅成本低；仍可演示可靠事件。缺点：进程级故障/伸缩共享，需架构测试防止大泥球。

## 拒绝方案

- 微服务：当前组织和运维成本不合理；
- 传统分层单体：边界容易退化。
