name: MCP protocol and security

on:
  workflow_call:
  push:
    branches: [main]
    paths:
      - 'backend/**'
      - 'keycloak-resource-binding/**'
      - 'deploy/**'
      - 'scripts/mcp_*.sh'
      - 'scripts/mcp_*.py'
      - '.env.example'
      - 'pom.xml'
      - 'Makefile'
      - '.github/workflows/mcp.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  production-security:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24.18.0'

      - name: Validate MCP verification scripts
        run: |
          shellcheck scripts/mcp_smoke.sh
          python3 -m py_compile scripts/mcp_auth_proxy.py

      - name: Verify MCP production security and official conformance
        run: make mcp-production-security

      - name: Upload MCP verification evidence
        uses: actions/upload-artifact@v7
        with:
          name: mcp-production-security
          path: |
            target/mcp-conformance/
            target/mcp-production-security/
          if-no-files-found: error
          retention-days: 30
