global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alert-rules.yaml

scrape_configs:
  - job_name: cellarbridge-backend
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: [backend:8080]
